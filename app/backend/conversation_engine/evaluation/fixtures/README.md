# Fixtures

Store deterministic parser fixtures here.

Suggested fixture shape:

- `state_name`
- `transcript`
- `expected_event`
- `expected_slots`
- `expected_cold_path`

Use only real, anonymized Hindi transcripts in Devanagari when fixtures are added.


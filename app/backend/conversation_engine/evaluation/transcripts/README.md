# Transcripts

Place anonymized real-call transcripts here before turning them into fixtures.

Recommended split:

- `raw/` for untouched anonymized transcripts
- `labeled/` for parser-training labels
- `failures/` for hot-path misses that required cold-path recovery

All Hindi text should remain in Devanagari.
